# === Environment ===
ENV=local  # local | staging | production

# === MLflow ===
MLFLOW_TRACKING_URI=file:./mlruns

# === Google AI (for synthetic ticket note generation — Phase 2) ===
GOOGLE_API_KEY=your-google-api-key-here
